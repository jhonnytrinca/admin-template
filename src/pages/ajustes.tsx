import type { NextPage } from 'next';
import Layout from '../components/template/Layout';

const Ajustes: NextPage = () => {
  return (
    <div>
      <Layout
        titulo='Ajustes e Configurações'
        subtitulo='Personalize o sistema por aqui!'
      >
        <h3>Conteúdo...</h3>
      </Layout>
    </div>
  );
};

export default Ajustes;
